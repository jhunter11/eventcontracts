strategy_id = "weather-threshold-v1"
name = "weather_threshold"
version = "0.1.0"
description = "Example bundle: threshold trade on Kalshi weather contracts informed by NWS observations."
feature_schema_id = "weather_threshold_features"

[subscription]
venues = ["kalshi"]
instrument_patterns = ["WEATHER-*"]
event_kinds = ["trade", "quote", "external_signal"]
external_sources = ["nws"]

[default_execution_priority]
tier = "standard"

[parameters]
buy_below = "0.40"
sell_above = "0.60"
size = "10"

[model]
name = "weather_threshold"
version = "v1"
format = "rules"
path = ""
sha256 = ""

[tags]
family = "weather"
mode = "paper"
