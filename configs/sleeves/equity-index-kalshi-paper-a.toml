sleeve_id = "equity-index-kalshi-paper-a"
strategy_id = "equity-index-range-ladder-v1"
strategy_version = "1.0.0"
venue = "kalshi"
capital_allocation = "3000"
currency = "USD"

[risk]
# Range ladders touch many brackets, so open-order headroom is larger while
# per-order and gross notional stay conservative for paper.
max_order_notional = "150"
max_position_notional = "1000"
max_daily_loss = "750"
max_open_orders = 20
max_gross_exposure = "1500"
currency = "USD"

[tags]
mode = "paper"
family = "equity"
